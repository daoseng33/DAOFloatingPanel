use_frameworks!

platform :ios, '9.0'

target 'DAOFloatingPanel_Example' do
  pod 'DAOFloatingPanel', :path => '../'

  target 'DAOFloatingPanel_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 2.2.0'
    pod 'Nimble', '~> 8.0.7'
  end
end
